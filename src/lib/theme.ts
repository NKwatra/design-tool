import { ITheme } from "../types/theme";

const theme: ITheme = {
  /**
   * This will the default stroke color for all items
   */
  itemDefaultColor: "#abafae",
  /**
   * This will the default stroke width for each item
   */
  itemStrokeDefaultWidth: 1,
  /**
   *  This will be the default color for text, for all items
   */
  itemTextDefaultColor: "#7e8d9f",
  /** This will be the default font size for text in items */
  itemTextFontSize: 14,
};

export default theme;
